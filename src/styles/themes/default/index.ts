import { createTheme } from '@stitches/react'

export const colors = {
  primary: '#652FFF',
  secondary: '#FFFFFF',
  tertiary: '#000000',

  success: '#31D0AA',
  error: '#FF4444',
  info: '#0EB2E5'
}

export const default_theme = createTheme('default_theme', { colors })
